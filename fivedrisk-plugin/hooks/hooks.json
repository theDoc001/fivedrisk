{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Edit|Write|WebFetch|Read",
        "hooks": [
          {
            "type": "command",
            "command": "python -m fivedrisk --format json score -"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "python -m fivedrisk --format json scan-output -"
          }
        ]
      }
    ]
  }
}
